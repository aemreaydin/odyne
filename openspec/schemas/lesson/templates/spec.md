<!-- Delta spec for the ENGINE capability this lesson builds (e.g. core-memory),
     not for the lesson. Merges into openspec/specs/ at archive — the engine's
     living specification. Use MODIFIED (with full requirement blocks) when
     changing requirements that already exist in openspec/specs/. -->

## ADDED Requirements

### Requirement: <!-- engine behavior, e.g. "Arena allocation" -->
<!-- SHALL/MUST statement of observable engine behavior -->

#### Scenario: <!-- scenario name -->
- **WHEN** <!-- condition -->
- **THEN** <!-- expected outcome -->
