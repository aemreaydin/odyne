<!-- TUTORED LESSON — CONSTITUTION (binding)
     Every task carries exactly one owner tag: [tutor] or [you].
     Agent: execute [tutor] tasks in order; HARD-STOP at the first incomplete
     [you] task — summarize state, say exactly what the learner should do, END TURN.
     NEVER write, complete, or check off a [you] deliverable.
     When the learner reports a [you] task done, verify the observable outcome
     (run `odin test`, build the app, check files) before marking it and continuing.
     Full constitution: .claude/skills/tutor/SKILL.md -->

<!-- BUILD lesson, interface: LEARNER-DESIGNED. Three parts: (1) graduate katas/timing/ into
     engine/ (kata stays in place, as m03-03 did), (2) build the fixed-timestep pacer +
     limiter, (3) rewire examples/testbed to the real loop.
     NOTE (Windows): the tier-2 harness and the testbed need SDL3.dll beside the binary —
     `cp $(odin root)/vendor/sdl3/SDL3.dll build/`. Carries the engine spec delta(s)
     into openspec/specs/ at archive — capability names follow the placement agreed at 2.2
     (expected: a time/clock capability for the graduated kata, plus the loop's own; MODIFIED
     blocks if an existing capability is touched). Demo checkpoint is part of done: paced
     loop, live readout, CPU no longer pegged, a 2 s hitch absorbed not fast-forwarded. -->

## 1. Orient

- [x] 1.1 [tutor] Verify/register cite-keys in BIBLIOGRAPHY.md (registered this lesson: `GAFFER-TIMESTEP`, `GPP-LOOP`, `DEWITTERS`, `UNITY-TIME`, `UE-SUBSTEP`, `MS-TIMEPERIOD`; widened: `HMH` day 18, `SDL` timer-resolution hint + `SDL_AppIterate`; already registered: `GEA` §8.4–8.5, `MS-QPC`, `ODIN-TIME`, `SOKOL`, `ND-FIBERS`); write lesson.md
- [x] 1.2 [you] Read lesson.md and the required reading (GAFFER-TIMESTEP all five sections; GPP-LOOP "Game Loop"; GEA §8.4–8.5); note questions

- [x] 1.3 [tutor] Lesson amendment (learner-asked, mid-build): lesson.md "In the industry" gained **"Pacing is a separate industry story from the fixed step"** — the four schools (display owns the clock / presentation timestamps / one-frame-old anchor / running grid with an explicit bound), the finding that no shipping approach trusts a long-lived deadline anchor, and the reframing that pacing optimises for consistency rather than for hitting a number. Prompted by the `loop/limiter_can_be_re_enabled_mid_run` failure. Registered: `ANDROID-PACING`, `UE-PACING`; widened: `UNITY-TIME` (`Application.targetFrameRate`). Reading list gained a pacing-specific "Deeper" line
- [x] 1.4 [tutor] Lesson correction, surfaced by probe P3: the "derive sim time" paragraph leaned on m11-01's f32 drift measurement to justify the derivation. Wrong argument - `sim_time += fixed_dt` in integer nanoseconds is exactly equal to `steps_taken * fixed_dt`, so exactness does not distinguish them. Rewritten to separate the two rules: the f32 number argues for the UNIT (integer ns), and the derivation is justified by single-source-of-truth (no second variable that can disagree with the step count). The probe was aimed at this and found it

## 2. Design

- [x] 2.1 [you] Sketch the design in design.md §Learner sketch (placement + package names, who owns the `for`, pacer state/API, sim rate + representation, catch-up bound + what happens to refused time, the input seam across 0/1/N steps, limiter policy on Windows, pause/scale decision, interpolation contract, demo readout)
- [x] 2.2 [tutor] Critique the sketch against the cited designs (Fiedler's accumulator, Nystrom's `render(lag/MS_PER_UPDATE)`, Unity's dt clamp, Unreal's substep cap, deWiTTERS' MAX_FRAMESKIP, HMH day 18's limiter) — 11 findings + proposed interface recorded in design.md; `core/time` ruled out by compiler probe (package names are program-wide unique; import identifier comes from the path segment); agreed interface BLOCKED on Q1–Q7
- [x] 2.3 [you] Answer Q1–Q7 in design.md (accepted: `timing`, driver in `game`, pacer takes a Duration, struct return, one Pacer instance, limiter mechanism in the driver, pause rules, alpha-only; input rule + finding-6 resolution confirmed after the round-2 walkthrough; **default sim rate chosen: `time.Second/50`**)
- [x] 2.4 [tutor] Record the agreed interface in design.md §Agreed interface (locked 2026-07-28 — 50 Hz exact-in-ns step, `clamp_dt` derived from `fixed_dt`, `max_dt` absolute 100 ms ⇒ 5-step worst case, `frame`/`step`/`render` callback split as the input-seam fix, deferrals recorded with trigger conditions)

- [x] 2.5 [tutor] Design amendment 1 (learner-raised): `App` holds `cfg` + `Frame_Clock` + `Pacer` + this frame's `Frame_Steps` instead of eleven flattened copies of their fields — one source of truth, no publish step, history stats computed on demand instead of ~25 ns/frame of scans nobody reads; `App_Config.window` → `initial_window` (a retained desc stops being true on the first resize); the separate `App.unlimited` folded into the resolved `cfg`. Trade recorded honestly (no `const` / no per-field privacy in Odin ⇒ a game *can* corrupt `app.clock`). Stub + 7 tier-2 cases updated by the tutor; pacer unaffected

- [x] 2.6 [tutor] Design amendment 2 (learner-raised): the driver's file is `engine/game/app.odin`, not `loop.odin` — `init`/`shutdown` are once-per-`run`, and a file called `loop` implied loop scope; matches the repo's file-per-type-family convention (`window.odin` → `Window_*`). `Loop_Error` → `App_Error` for the same reason (its members are startup errors, not loop errors)

- [x] 2.7 [tutor+you] Design amendment 3 - the limiter's deadline anchor. Found by test (`loop/limiter_can_be_re_enabled_mid_run`: **353 ms stall after 20 unpaced frames** at a 16.67 ms target), not by reading. Root cause: an origin-anchored grid is only valid while every frame is paced to it; unpaced frames burn grid slots without spending wall time (and a live `target_fps` change respaces every past frame, which corrects an earlier tutor claim). Learner chose the **one-frame-old anchor** (`clock.prev + period`, HMH day 18's shape) from three options; re-anchoring grid and presentation timestamps recorded as rejected-with-triggers. Cost recorded: overshoot is no longer corrected, so the achieved rate settles marginally below target - 6.1(b) now measures the slippage. **⚠️ Tutor wrote this implementation at the learner's explicit request (hint-ladder rung 4): two lines of `run` plus their doc comment. Flagged for the review self-check.** Spec delta's Frame pacing requirement rewritten + a re-enable scenario added

## 3. Spec

- [x] 3.1 [tutor] Write the engine spec delta(s) at the agreed placement — TWO new capabilities: `specs/core-timing/spec.md` (monotonic frame clock incl. m11-01's deferred delta, spike clamping with the raw delta preserved, fixed-capacity history, deadline waiting, fixed-timestep pacing, conservation of fed time, publishable phase) and `specs/game-loop/spec.md` (engine-owned loop, invariant frame phase ordering, fixed-step-only simulation, bounded hitch absorption, pacing, ZII config, pause)
- [x] 3.2 [tutor] Write stubs at the agreed interface — `engine/core/timing/pacer.odin` (Pacer, Frame_Steps, pacer_init/advance/alpha, sim_time, step_seconds) and `engine/game/app.odin` (App_Config, App, App_Callbacks, App_Error, run). Benign-value bodies, not `unimplemented()`; both compile clean under the project flag set
- [x] 3.3 [tutor] Write failing tests, two tiers per the project convention: **tier 1** `engine/core/timing/pacer_test.odin` — 13 tests, fabricated deltas only, no clock (defaults + 50 Hz exactness, 0-step frames, single/multi-step, the `>=` boundary to the nanosecond, the 5-step catch-up bound, zero-delta pause, phase progression + query agreement, conservation + phase range over 100,000 xorshift-jittered frames, chunk-independence of derived sim time). **Tier 2** `tests/platform/suite_loop.odin` — 7 real-window cases (close-request exit, callback ordering + reported-count agreement, fixed step dt with 0-step and multi-step frames, 2 s hitch absorbed with raw_dt observable, pause freezes sim not real + no replay on resume, limiter never faster than target vs unlimited, optional callbacks). **Verified RED 2026-07-28:** 13/13 tier-1 fail, 7/7 tier-2 fail, no panics, no leaks, `-vet -strict-style -warnings-as-errors` clean, and the 29 pre-existing tier-2 cases plus all 9 other test packages still green

- [x] 3.4 [tutor] Test tooling (learner-requested): `scripts/test.ps1` now runs BOTH tiers — `odin test` packages plus every executable package under `tests/` (built to build/tests/, then run), with `-InProcess` to pass the harness `--in-process`; `Get-OdinPackage` gained `IsHarness`, and a new `Sync-SdlRuntime` copies SDL3.dll beside the harness binary on Windows (without it a correctly linked binary dies at load). Verified: 11 packages discovered, tier-1 and tier-2 both reported in one summary
- [x] 3.5 [tutor] Follow-up test — the interpolation-phase invariant at the top of its range (`test_phase_stays_below_one_at_the_very_top_of_the_range`, plus a strict `< 1` assertion added to the boundary test). Written because the tolerance-based checks were MASKING a real defect: `f32(19_999_999)` rounds to 20,000,000 (f32 spacing is 2 above 2^24), so a phase divided in f32 returns exactly 1.0 and breaks the [0,1) contract the render path depends on. Verified RED against the learner's implementation, at k = 1 ns only

- [x] 3.6 [tutor] Harness watchdog (learner-approved): each child process starts a thread that kills it with exit code 124 after `CASE_TIMEOUT_SECONDS` (`#config(ODYNE_CASE_TIMEOUT, 15)`), so a loop with no exit condition FAILS instead of hanging the run; the verdict line moved from child to parent, so a case that aborts on an assertion still gets one. Verified by forcing it (`-define:ODYNE_CASE_TIMEOUT=1` against the 2 s hitch case), and it then caught two real hangs in a row
- [x] 3.7 [tutor] Two corrections to the tutor's own tests, both found by running them against the learner's implementation: `step_dt_is_always_the_fixed_step` configured `fixed_dt = 1s` against a 100 ms `max_dt` - the exact degenerate config the learner's new assertion correctly rejects (now 50 ms); and `pause_freezes_sim_not_real` asserted the resume frame takes ZERO steps, which a correct loop fails - the resume frame is entitled to its own duration (~3 steps), and the real distinction is ~3 vs ~30. Replaced with `steps_during_pause == 0` plus a resume bound, and the reason the pause must be shorter than the clamp threshold is now written in the test
- [x] 3.8 [tutor] ASCII-only in printed strings across the harness and both test suites (learner: non-ASCII does not render in the Windows terminal)

## 4. Build

- [x] 4.1 [you] Graduate the timing kata into `engine/` at the agreed placement; existing timing tests carried over and green (verified 2026-07-28: `engine/core/timing/{timing,pacer}.odin` + both test files, `katas/timing/` left in place as m03-03 did, 18 clock tests carried over green)
- [x] 4.2 [you] Implement the pacer + limiter until `odin test` is green and the leak check is clean (verified 2026-07-29: **11/11 test packages green, 36/36 tier-2 cases green**, leak-clean, vet/strict-style clean). Debugging journey worth keeping: nil-callback segfault and a `for` with no exit condition (both caught as a crash and a hang); `clamp_dt` resolved from the *unresolved* `cfg.fixed_dt` so a hitch reported dt 0; pause first guarded the WHOLE frame body, which deadlocked the loop (no pump => no close, no `cb.frame` => nothing could clear `paused`); and the period computed three ways before landing - `Duration(target_fps)` = 60 ns, then f64 milliseconds, then `period * time.Millisecond` = 16,666 s/frame. Last one is the lesson's unit trap in its sharpest form: **`Duration * Duration` is ns-squared and `distinct i64` cannot catch it**
- [x] 4.3 [you] Wire the demo checkpoint - `examples/testbed` runs the real loop with the readout (frame index, fps, frame-time avg/min/max, steps, alpha, sim vs real, PAUSED/UNCAPPED), Esc closes, P pauses, H injects a 2 s hitch, L toggles the limiter, and a raw-vs-lerp pair exercises the interpolation contract (verified 2026-07-29: builds clean under the full flag set; learner confirmed H/L/P and the lerp by observation, incl. the reversed-lerp bug found at review and fixed). Tutor-authored: the fps + state-flag lines

## 5. Review

- [x] 5.1 [tutor] Verify green + demo + build; review per references/review-rubric.md; ask >=2 comprehension probes (done 2026-07-29: 22/22 build, 11/11 test packages, 38/38 harness cases incl. `-InProcess`, leak-clean, graduated kata byte-identical to katas/timing/, SDL confined to platform, layering clean. **7 findings:** stale `App.steps` on paused frames [enforced by a new red test - 40 phantom steps], clock origin predating `cb.init`, unguarded `target_fps`, untested `App_Error` branches, dead accessors, loop-invariant recomputation, close-check-before-pump [noted only]. **3 probes** posed: the input seam under 0/2-step frames, where 0.1%-slow pacing is a real bug, and whether the f32 drift measurement actually argues for deriving sim time. Tutor self-check answered: yes, tutor wrote implementation code - see design.md amendments 3 and 4)
- [x] 5.2 [you] Answer the probes; address review findings (findings all closed 2026-07-29 but by the TUTOR at the learner's reaffirmed request - design.md amendment 4; 38/38 green. **Probes answered with corrections, worth carrying into the journal:** P1 - learner proposed choosing a rate (60, then sim==target) twice; both wrong, and matching the rates is provably worse (every frame is period+overshoot, so the leftover reaches a full step periodically and one frame runs two). Answer taught: match the CARDINALITY of the consumer to the cardinality of the event - edges are one-per-frame, `frame` is one-per-frame, `step` is 0..N; a step that must act on an edge gets it via a latch. P2 - learner said sim/real divergence 'will affect physics calculations', which inverts what the fixed step buys: physics is bit-identical regardless of step rate, divergence means slow motion, and the real casualty is agreement with an EXTERNAL clock (audio at 48 kHz starves by one 1024-sample buffer every ~21 s at 0.1% slow). P3 - property correct but did not distinguish integer accumulation from derivation; second half found a real error in lesson.md, fixed at 1.4)

## 6. Finalize

- [x] 6.1 [tutor] Run the measurement task; record Built + Measured in curriculum/JOURNAL.md (done 2026-07-29: `katas/main_loop_bench/` with per-section process isolation, six sections. Headline findings: SDL owns the Windows timer resolution (13.02 ms -> 0.955 ms from platform.init alone, ours adds nothing); Windows REVOKES it mid-run after ~8-16 s and timeBeginPeriod cannot restore it, which invalidated the bench's first single-process design; `time.sleep` returns EARLY at 16.7 ms due to whole-ms truncation; the software limiter achieves only 44-46 fps against a 60 fps target with 29-38% slippage, correcting amendment 3's '0.1%' estimate by ~40x; steps-per-frame matches 50/render_rate to three decimals; i64 accumulation is exact so deriving is about single-source-of-truth not precision; the rejected step cap puts alpha at 54.17; loop bookkeeping is ~126 ns/frame)
- [x] 6.2 [tutor] Walk the learner through what the numbers mean (delivered with the 6.1 results)
- [x] 6.3 [you] Write Takeaways + Reflections in curriculum/JOURNAL.md (your own words) (verified 2026-07-29)
- [x] 6.4 [tutor] Copy lesson.md -> curriculum/modules/m11/m11-02-main-loop/LESSON.md (done 2026-07-29, incl. the 1.3 pacing-industry section and the 1.4 derive-vs-accumulate correction)
- [x] 6.5 [tutor] Update curriculum/curriculum.yaml (m11-02 -> done; m20-01 -> available; active_change cleared)
- [x] 6.6 [tutor] Commit the lesson; hand off to /opsx:archive (committed 925da40; specs core-timing + game-loop synced into openspec/specs/ - including m11-01's deferred frame-clock delta; archived 2026-07-29)
